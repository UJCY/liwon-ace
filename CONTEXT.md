# Ubiquitous Language — Company-X MCP 검색 시스템

2026 오픈소스 개발자대회 지정과제 "MCP 기반 지능형 데이터 플랫폼 클러스터"의 용어집.

## 용어

### MCP 서버
과제의 핵심 산출물. MCP 프로토콜로 도구 4개(라우터 + 도구 3종)를 등록해 노출하는 서버.

### 도구 3종
과제가 요구하는 3개의 MCP 도구. 각각 하나의 데이터 자산에 대응한다.

| 도구 | 대응 데이터 | 역할 |
|------|-----------|------|
| 벡터 검색 | 문서 40건 (Markdown) | 비정형 문서 의미 검색 |
| NL2SQL | 정형 테이블 8개 (~800행) | 자연어를 SQL로 변환해 정형 데이터 질의 |
| 지식 그래프 | 노드 133 / 엣지 354 (JSON) | 개체 간 관계 탐색 (온톨로지 기반) |

### 라우터 (ask)
MCP 도구로 등록되는 게이트웨이. 자연어 질문을 받아 **규칙 기반**으로 질문 유형을 분석하고, 도구 3종 중 적합한 도구를 내부에서 선택·호출한다. 에이전트는 평소 라우터만 호출한다. LLM이 도구를 고르는 방식이 아니다 — 선택은 규칙이 한다.

### MCP Parallel 패턴
라우터가 도구를 선택·호출하는 방식의 이름 (KOSSA 과제 문서 용어). 필요 시 도구 여러 개를 병렬 호출해 결과를 병합하는 패턴.

### 에이전트
Ollama 로컬 소형 LLM 기반 AI 에이전트. 사용자 질문을 받아 MCP로 라우터를 호출하고, 반환된 데이터로 최종 답변을 생성한다. 외부 API(OpenAI, Claude 등) 사용 불가.

### 예시 질문 (questions.json)
도구별 10개씩 총 30개의 질문 + 기대 도구 + 힌트. 전부 정상 매칭 질문이며, **회귀 전용** — 규칙 튜닝에는 쓰지 않고 설계 변경이 정상 케이스를 깨지 않는지 확인하는 용도로만 쓴다.

### 무매칭 / 애매
라우터 판정의 두 경계 상태. **애매**는 후보 도구가 있으나 확신이 없는 상태(→ 병렬 호출 후 병합), **무매칭**은 후보 도구 자체가 없는 상태(→ 거절 응답). 서로 다른 처리를 받는다.

### 거절 응답
무매칭 질문이나 데이터 부재 시 반환하는 구조화 응답. 시스템 오류와는 구분되는 타입이며, 에이전트는 이를 받으면 모른다고 답한다 (환각 방지).

### 구조 엣지케이스 / 인스턴스 과적합
**구조 엣지케이스**는 데이터셋 내용을 몰라도 성립하는 시스템 실패 유형 — 분석 대상. **인스턴스 과적합**은 개별 질문·문서에 맞춘 튜닝 — 금지.

### 자체 엣지 세트
구조 엣지케이스 검증을 위해 팀이 직접 작성하는 질문 세트. 케이스 유형 기반으로 작성하며 데이터셋 인스턴스에서 역산하지 않는다.

### TACC (선택적 컨텍스트 큐레이션)
필수 참조 논문 [1]의 기법. 컨텍스트를 무조건 많이 제공하지 않고 모델 특성과 과업 유형에 따라 선별 구성하는 것.

### Company-X
데이터셋의 가상 IT 솔루션 기업. 직원 45명, 고객사 30개, 데이터 기간 2024-01 ~ 2026-06.
