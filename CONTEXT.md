# Ubiquitous Language — Company-X MCP 검색 시스템

2026 오픈소스 개발자대회 지정과제 "MCP 기반 지능형 데이터 플랫폼 클러스터"의 용어집.

## 용어

### MCP 서버
과제의 핵심 산출물. MCP 프로토콜로 도구 4개(라우터 + 도구 3종)를 등록해 노출하는 서버.

### 도구 3종
과제가 요구하는 3개의 MCP 도구. **질문이 무엇을 돌려받길 요구하는가**로 갈린다 — 데이터 자산으로 가르지 않는다 ([요구 출력 형태](#요구-출력-형태) 참조).

| 도구 | 돌려주는 것 | 대응 데이터 |
|------|-----------|-----------|
| 벡터 검색 | **서술** — 원인, 방법, 절차, 결정, 제안 | 문서 40건 (Markdown) |
| NL2SQL | **정형 속성·수치** — 금액, 연봉, 기간, 상태, 행 개수 | 정형 테이블 8개 (818행) |
| 지식 그래프 | **개체 식별·연결** — 누가 무엇을 쓰는가, 누구 소속인가 | 노드 133 / 엣지 354 (JSON) |

### 요구 출력 형태
도구 경계를 가르는 기준. 질문이 **어느 자산에 있는가**가 아니라 **어떤 형태의 답을 요구하는가**로 판별한다.

자산 기준을 쓰지 않는 이유는 [자산 중복](#자산-중복)이다. 지식 그래프는 정형 테이블에서 100% 도출되고, 문서는 40건 전부가 그래프 개체를 언급한다 ([dataset-analysis.md](./docs/dataset-analysis.md)).

### 자산 중복
세 데이터 자산이 같은 사실을 다른 형태로 담고 있는 구조적 성질. 이 데이터셋의 결함이 아니라 **성질**이며, 라우터 설계의 전제다.

중복에는 두 종류가 있고 처리가 다르다.

| 종류 | 예 | 두 도구의 답 | 처리 |
|------|-----|------------|------|
| **동일 중복** | 관계를 세는 집계, 관계를 따라가는 조회 | **같다** (그래프가 테이블의 파생) | 단일 선택 — 병렬하면 같은 답이 두 번 |
| **상보 중복** | 사건 조회, 개체 지목 서술 | **다르다** (티켓=사건 목록 / 문서=원인 서술) | 병렬 호출 후 병합 |

케이스 카탈로그는 [edge-cases.md](./docs/edge-cases.md) X1~X4.

### 라우터
MCP 도구로 등록되며, 자연어 질문을 받아 **규칙 기반**으로 [요구 출력 형태](#요구-출력-형태)를 판별해 도구 3종 중 적합한 것을 정한다. 에이전트가 아니라 서버에 있고, LLM이 도구를 고르는 방식이 아니다 — 선택은 규칙이 한다.

판별 이후 동작은 **배치 형태에 따라 다르며, 공통부 구현 후 재평가로 미뤄져 있다** ([routing-topology.md](./docs/routing-topology.md)):

- **게이트웨이형(A안)** — 라우터 자신이 도구를 호출하고 결과까지 반환한다. 도구 이름은 `ask`. 라우터가 **실행자**다.
- **조언자형(B안)** — 라우터는 어느 도구를 어떤 인자로 부를지만 반환하고, 실제 호출은 에이전트가 한다. 도구 이름은 `route`. 라우터가 **조언자**다.

### MCP Parallel 패턴
KOSSA 요강이 "규칙 기반 라우터를 통한 도구 자동 선택"에 붙인 명칭 (요강 원문의 괄호 표기).

**원문은 이 패턴이 무엇인지 정의하지 않는다.** 출처 3곳(요강·블로그·공지)과 데이터셋 전수 검사에서 "병렬"은 0회, "Parallel"은 이 한 곳뿐이다. 블로그의 라우터 서술(*"적합한 도구를 자동 매칭"*)은 **수를 명시하지 않는다** — 한국어는 복수 표지가 선택적이다. 도구 1개를 못 박는 것은 문면이 아니라 스키마다: 예시 질문 30개의 `tool`이 배열이 아니라 문자열이다 ([dataset-analysis.md](./docs/dataset-analysis.md) 6장).

→ **여러 도구를 동시에 호출하는 것은 과제 요구가 아니라 우리 설계 선택이다.** 우리 설계에서 그 동작을 가리키는 말은 [병렬 호출](#병렬-호출)이며, 이 용어와 구분한다.

### 병렬 호출
우리 설계 용어. [상보 중복](#자산-중복) 구간에서 후보 도구 여럿을 동시에 호출하고 결과를 병합하는 동작. 발동 조건은 규칙에 명시된 중복 유형이며, 확신 부족만으로는 발동하지 않는다.

### 에이전트
Ollama 로컬 소형 LLM 기반 AI 에이전트. 사용자 질문을 받아 MCP로 라우터를 호출하고, 반환된 데이터로 최종 답변을 생성한다. 외부 API(OpenAI, Claude 등) 사용 불가.

### 예시 질문 (questions.json)
도구별 10개씩 총 30개의 질문 + 기대 도구 + 힌트. **회귀 전용** — 규칙 튜닝에는 쓰지 않고 설계 변경이 기존 동작을 깨지 않는지 확인하는 용도로만 쓴다.

**전부가 정상 매칭 질문은 아니다.** 대조 결과 결함 2건이 있다 — #5는 동일 구조 질문과 배정이 어긋나고, #23은 존재하지 않는 개체명을 참조한다 ([dataset-analysis.md](./docs/dataset-analysis.md) 5장). 그래서 회귀는 [2축](#회귀-2축)으로 잰다.

### 회귀 2축
questions.json을 하나의 점수로 보지 않고 두 축으로 나눠 재는 방식.

| 축 | 무엇을 재나 | 기대값 |
|---|---|---|
| **라우팅 축** | 라우터가 고른 도구가 기대 도구와 같은가 | **29/30** (#5는 문서화된 알려진 불일치) |
| **실행 축** | 선택된 도구가 올바른 응답 유형을 내는가 | **30/30** (#23의 정답은 결과가 아니라 개체 부재 응답) |

"라우팅이 틀린 것"과 "라우팅은 맞고 데이터가 없는 것"은 다른 실패다 — 이 구분은 [거절 응답](#거절-응답) 규약의 연장이다.

### 무매칭 / 애매 / 부분 응답
라우터 판정과 도구 실행의 세 경계 상태. 서로 다른 처리를 받는다.

| 상태 | 정의 | 처리 |
|------|------|------|
| **무매칭** | 후보 도구 자체가 없다 (스몰토크, 데이터 범위 밖) | 도구를 호출하지 않고 [거절 응답](#거절-응답) |
| **애매** | 후보 도구가 여럿이고 [상보 중복](#자산-중복) 구간에 해당한다 | [병렬 호출](#병렬-호출) 후 병합 |
| **부분 응답** | 도구는 맞게 골랐고 개체도 존재하는데, **요구한 형태의 답만 그 자산에 없다** | 아래 참조 |

### 부분 응답
질문이 요구한 [출력 형태](#요구-출력-형태)를 담은 자산에는 자료가 없고, 다른 자산에 **인접 사실**만 있는 상태.

예: 문서가 없는 고객사의 "장애 원인"을 물으면 — 원인(서술)은 없고, 그래프에는 이슈를 보고했다는 사실이 있다.

응답은 **"요구 유형의 자료 없음"과 "확인된 인접 사실"을 구조화 필드로 분리**해 반환한다. 에이전트는 이를 읽고 인접 사실을 요구된 답인 것처럼 말하지 않는다. 이것이 없으면 모델이 인접 사실로부터 답을 지어낸다.

데이터 부재(자료가 아예 없음)와도, 거절(후보 도구가 없음)과도 다른 상태다.

### 거절 응답
무매칭 질문이나 데이터 부재 시 반환하는 구조화 응답. 시스템 오류와는 구분되는 타입이며, 에이전트는 이를 받으면 모른다고 답한다 (환각 방지).

### 구조 엣지케이스 / 인스턴스 과적합
**구조 엣지케이스**는 데이터셋 내용을 몰라도 성립하는 시스템 실패 유형 — 분석 대상. **인스턴스 과적합**은 개별 질문·문서에 맞춘 튜닝 — 금지.

경계선: **자산 간 구조를 대조하는 것은 허용**이고(무엇이 어디에 중복되는지), **개별 질문에서 규칙을 역산하는 것은 금지**다. [자산 중복](#자산-중복) 분석은 전자에 속한다.

### 자체 엣지 세트
구조 엣지케이스 검증을 위해 팀이 직접 작성하는 질문 세트. 케이스 유형 기반으로 작성하며 데이터셋 인스턴스에서 역산하지 않는다.

### TACC (선택적 컨텍스트 큐레이션)
필수 참조 논문 [1]의 기법. 컨텍스트를 무조건 많이 제공하지 않고 모델 특성과 과업 유형에 따라 선별 구성하는 것.

### Company-X
데이터셋의 가상 IT 솔루션 기업. 직원 45명, 고객사 30개, 데이터 기간 2024-01 ~ 2026-06.
