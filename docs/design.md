# 설계 결정 — Company-X MCP 검색 시스템

> 과제 원문 요구는 [requirements.md](./requirements.md), 용어는 [/CONTEXT.md](../CONTEXT.md), 각 결정을 뒷받침하는 관련 연구는 [references/related-work.md](./references/related-work.md) 참조.
> 이 문서는 **우리 팀이 내린 결정**과 그 이유, 아직 열어둔 항목을 기록한다. (2026-07-30 그릴 세션 결과)

## 아키텍처 한 장

> **라우터 배치가 미확정이라 두 형태가 열려 있다.** 아래 A/B 중 선택 필요 — 비교는 [routing-topology.md](./routing-topology.md), 항목은 "열어둔 항목"의 **라우터 배치**.

**A안 — 게이트웨이형** (라우터가 도구를 직접 실행하고 결과까지 반환, MCP 왕복 1회)

```
사용자 질문
   ↓
AI 에이전트 (Ollama 소형 LLM)
   ↓  MCP 호출: ask(question)          ← 왕복 1회
MCP 서버 ─ 등록 도구 4개
   ├─ ask            ← 게이트웨이 (규칙 기반 라우터 내장)
   │    ├─ 규칙 매칭 확실 → 도구 1개 호출
   │    └─ 애매(동률·미달) → 후보 도구 병렬 호출 후 병합  ← MCP Parallel 패턴
   ├─ vector_search  ← 직접 호출도 가능 (검증·시연용)
   ├─ nl2sql
   └─ knowledge_graph
   ↓
PostgreSQL (+ pgvector)   ← 문서 청크·정형 테이블·그래프 전부 여기
   ↓
에이전트 LLM이 반환 컨텍스트로 최종 답변 생성
```

**B안 — 조언자형** (라우터는 어느 도구를 쓸지만 알려주고, 에이전트가 그 도구를 호출, MCP 왕복 2회)

```
사용자 질문
   ↓
AI 에이전트 (Ollama 소형 LLM)
   ↓  ① route(question)                ← 왕복 1
MCP 서버 ─ route: 규칙 판별만 수행
   ↓  {tool:"nl2sql", args:{...}, confidence:0.9}  또는  {out_of_scope}
에이전트 (지시대로 호출 — 코드가 실행하는 B2 권장, LLM이 고르는 B1은 비권장)
   ↓  ② nl2sql(args)                   ← 왕복 2
MCP 서버 ─ 도구 3종 (실사용 경로)
   ↓
PostgreSQL (+ pgvector)
   ↓
에이전트 LLM이 결과로 최종 답변 생성
```

## 확정 결정

### D1. 라우터는 MCP 서버 측 도구다 (배치 형태는 미확정)

라우터를 **에이전트 밖, MCP 도구로 둔다**는 것까지는 확정이다. 규칙 기반 판별 로직이 MCP 서버에 있고 에이전트에는 없다.

- **이유**: 데이터셋 README의 도구 매핑 표가 라우터를 "MCP 도구"로 표기한다. 소형 LLM에게 도구 선택을 맡기지 않는다는 요건(과제의 "규칙 기반" 문면)도 이 배치로 충족된다.
- **기각한 대안**: 라우터를 MCP 밖 에이전트 측 모듈로 두기 (README 표기와 어긋남), LLM 도구 선택 + 규칙 폴백 ("규칙 기반" 문면과 어긋남).
- **미확정 부분**: 그 라우터가 **도구를 직접 실행하는지(A안)**, **어느 도구를 쓸지만 알려주는지(B안)**. 두 안 모두 위 확정 사항을 만족한다. 비교는 [routing-topology.md](./routing-topology.md) — 핵심 트레이드오프는 *A안의 단순함·병렬 처리* 대 *B안의 관측성·검증 용이성*이다.

### D2. 등록 도구는 4개 — `ask` + 3종

라우터 도구(`ask` 또는 `route`) + `vector_search`, `nl2sql`, `knowledge_graph` 전부 MCP 도구로 등록. **총 4개라는 점은 D1의 A/B 선택과 무관하게 확정**이다.

- **A안이면**: 에이전트는 평소 `ask`만 호출하고, 3종은 개별 검증·시연·심사용 창구다.
- **B안이면**: 3종이 에이전트의 **실사용 호출 경로**가 된다. 등록 명분이 더 분명해진다.

- **이유**: 블로그 문면 "3개의 MCP 도구를 구현합니다"와 README 표(라우터 포함 4개를 MCP 도구로 표기)를 동시에 충족. `ask`만 노출하면 도구 3종 구현 요구 미충족으로 해석될 심사 리스크.
- 도구 식별자는 questions.json의 `tool` 값(`nl2sql`, `vector_search`, `knowledge_graph`)과 일치시킨다.
- **부수 효과 — 에이전트 입장에서 선택지가 1개다**: 에이전트는 평소 `ask`만 호출하므로 도구 선택 문제를 겪지 않는다. Mem2ActBench(arXiv:2601.19935) Table 5는 후보 도구 수가 1→2→5로 늘 때 hard negative 조건에서 선택 정확도가 94.5%→78.0%→69.75%로 떨어짐을 보인다. **다만 같은 실험에서 random negative는 무변화(93.5~95.5%)였다 — 원인은 개수 자체가 아니라 의미적으로 겹치는 후보의 존재다.** 따라서 논거는 "도구를 적게 유지한다"가 아니라 **"에이전트에게 선택을 요구하지 않고, 노출하는 3종은 서로 데이터 자산이 겹치지 않게 설계한다"**로 쓰는 것이 문헌상 정확하다. 자세한 근거는 [references/related-work.md](./references/related-work.md) 5장

### D3. 병렬 정책 — 기본 단일, 애매하면 병렬

규칙 매칭이 확실하면 도구 1개만 호출. 스코어 동률·기준 미달이면 후보 도구를 병렬 호출해 결과를 병합한다.

**병렬 호출을 누가 수행하는지는 D1의 A/B 선택에 종속된다** — A안이면 `ask` 내부에서, B안이면 `route`가 후보 도구 목록을 반환하고 에이전트가 병렬 호출한다. 정책(기본 단일, 애매하면 병렬) 자체는 두 안 모두 동일하다.

- **이유**: questions.json은 질문당 기대 도구 1개 → 단일 선택이 기본 검증 단위. "MCP Parallel 패턴" 요구는 애매 케이스의 병렬 폴백으로 충족. 상시 병렬은 비용 3배에 "도구 자동 선택" 취지 훼손.
- **주의**: 라우터 규칙을 questions.json 30개에 과적합시키지 않는다. 30개는 검증용이지 학습용이 아니다.

### D4. NL2SQL — 도구 내부에서 LLM이 SQL 생성

`nl2sql` 도구가 Ollama를 호출해 스키마 + 질문 프롬프트로 SQL을 생성하고, 실행 후 결과를 반환한다. SELECT만 허용.

- **이유**: 게이트웨이 구조상 `ask`는 자연어만 받으므로 SQL 변환은 서버 쪽에서 일어나야 함. 규칙/템플릿 변환은 데이터셋 과적합이라 배제(팀 방침). 질문 유형별로 스키마를 선별해 프롬프트에 넣는 지점이 TACC 적용 포인트.

### D5. 지식 그래프 — PostgreSQL 테이블 + 재귀 CTE

nodes.json/edges.json을 PostgreSQL 테이블(nodes, edges)로 적재하고, 다단계 관계 탐색은 recursive CTE로 구현.

- **이유**: 과제 흐름도상 모든 도구가 PostgreSQL로 향함. "PostgreSQL 기반 지능형 데이터 플랫폼" 취지 부합, 추가 인프라 없음. Apache AGE는 RAM 4GB 제약에 부담, 인메모리 JSON은 플랫폼 그림에서 벗어남.
- "온톨로지 기반" 요구는 그래프 스키마 문서(노드 5유형, 관계 7종)를 온톨로지 정의로 명시하는 것으로 대응.

### D6. 무매칭 질문은 정직한 거절

라우터에 후보 도구가 없는 질문(스몰토크, 데이터 범위 밖)은 도구를 호출하지 않고 구조화 거절 응답(`out_of_scope`)을 반환한다. 에이전트는 이를 받아 모른다고 답변한다.

- **이유**: 무관한 컨텍스트를 소형 LLM에 주면 환각 유발. **애매**(후보 있음, 확신 없음 → D3 병렬)와 **무매칭**(후보 없음 → 거절)은 다른 케이스.
- **기각한 대안**: 무매칭 시 전 도구 병렬 폴백 (스몰토크에 DB 3회 조회 + 환각 위험), 최근접 도구 강제 라우팅.

### D7. 엣지케이스는 구조 레벨만 분석, 검증은 이원화

엣지케이스 분석은 **구조 레벨**(데이터셋 내용과 무관한 시스템 실패 유형)만 허용하고, 인스턴스 레벨(개별 질문·문서에 맞춘 튜닝)은 금지한다. 검증 세트는 둘로 나눈다: 자체 작성 엣지 세트(케이스 유형별) + questions.json(회귀 전용, 규칙 튜닝 사용 금지).

- **이유**: 엣지 견고성과 과적합 방지 동시 충족. questions.json 30개는 전부 정상 매칭 질문이라 엣지 검증 능력이 없음 — 자체 세트가 어차피 필요.
- 케이스 카탈로그: [edge-cases.md](./edge-cases.md) (라우터 경계 / 도구 실패 / 응답 품질 3개 카테고리).

### D8. 구현 스택 — 공식 MCP SDK 직접 사용 (air 미채택)

TypeScript + `@modelcontextprotocol/sdk` 1.30.0. 주최사 권장 프레임워크인 air(`@airmcp-dev/core` 0.3.0)는 채택하지 않는다. 프로토콜 리비전은 SDK가 지원하는 최신인 **`2025-11-25`**.

- **이유**: air 채택을 정당화하던 유일한 실질 기능은 **7-Layer Meter**(필수 참조 [2] Pylon-7의 계층을 계측)였는데, **실측에서 동작하지 않음을 확인했다.** `defineTool`에 `layer`를 명시해도 `meter-middleware.js`의 `classify()`가 도구 이름·파라미터만 보고 `layer` 속성을 무시한다(타입과 공식 문서에는 있는데 구현에 없다). 자동 분류 규칙도 완전 일치 정규식이라 우리 도구명이 하나도 걸리지 않아 모든 호출이 L4로 뭉개진다. 검증 상세는 [air-evaluation.md](./air-evaluation.md) 8절
- 도구가 4개뿐이라 보일러플레이트 절감은 결정 근거가 되지 못하고, 플러그인 19종 중 우리에게 맞는 것은 timeout·dedup 둘뿐이다(`retryPlugin`은 T1 제약과 충돌).
- 과제 문서가 air를 **"사용 의무는 없습니다"**로 명시하므로 미채택에 규정 위반은 없다. air를 검토하고 근거를 들어 기각한 기록 자체가 심사 서술이 된다.
- **기각으로 잃는 것과 대안**: Pylon-7 계층 계측은 포기하지 않는다. 도구 핸들러를 감싸는 얇은 래퍼에서 계층을 상수로 붙여 기록하며, air의 이름 패턴 추측보다 논문 계층 정의에 맞춰 우리가 명시하는 편이 정확하다 — `ask` = L5 Routing, 도구 3종 = L3 Resource.
- **부수 확인**: SDK 1.30.0의 `LATEST_PROTOCOL_VERSION`이 `2025-11-25`라, 스펙 문서상 최신인 2026-07-28 리비전은 **어떤 스택을 골라도 구현할 수 없다**. air의 리스크가 아니라 생태계 상태였다 ([mcp-spec.md](./references/mcp-spec.md) 교정 반영).

## 도구별 실제 예시 (questions.json 기반)

| 질문 | 라우터 선택 | 처리 |
|------|-----------|------|
| "서울 지역 매출 상위 5개 고객사를 알려줘" | `nl2sql` | LLM이 SQL 생성 (sales+clients 조인, region='서울', GROUP BY, LIMIT 5) → 실행 → 행 반환 |
| "최근 서버 장애 사례와 원인을 알려줘" | `vector_search` | 질문 임베딩 → document_chunks 유사도 검색 (장애보고 문서) → 상위 청크 반환 |
| "Client-A가 사용 중인 제품 목록은?" | `knowledge_graph` | client 노드에서 `USES` 엣지 탐색 → 제품 노드 목록 반환 |

각 경우 `ask`가 도구 결과를 컨텍스트로 반환하면 에이전트 LLM이 최종 답변 문장을 생성한다.

## 열어둔 항목

| 항목 | 상태 | 메모 |
|------|------|------|
| **라우터 배치 (A안 vs B안)** | **미정 — 선택 대기 (최우선)** | 게이트웨이형(라우터가 도구 실행, 왕복 1회) vs 조언자형(라우터가 지시만, 왕복 2회). 비교는 [routing-topology.md](./routing-topology.md). **D2·D3의 세부와 에이전트 구조가 여기에 종속되므로 구현 전 반드시 먼저 정해야 한다.** 트레이드오프: A안의 단순함·병렬 처리 대 B안의 관측성·검증 용이성 |
| ~~구현 스택~~ | **확정 (D8)** | TypeScript + `@modelcontextprotocol/sdk` 1.30.0 직접 사용. air 미채택 — 근거는 아래 D8 |
| MCP transport | 미정 | stdio로 시작. 심사 시연에서 원격 접근이 필요하면 Streamable HTTP 추가 검토. 프로토타입에서 stdio 동작 확인됨 |
| 임베딩 모델 | 기본값 후보 | 공지 예시 nomic-embed-text (Ollama). 확정은 스택 결정 시 |
| 청킹 전략 | 미정 | 문서 40건 규모라 단순 전략으로 시작. 과적합 방지 차원에서 문서별 튜닝 안 함 |
| LLM 모델 | 기본값 후보 | Gemma 4 E2B (블로그·공지 권장). KOSSA "7B" 표기와 차이 → 주최 측 문의 후보 |
| TACC 적용 세부 | 방향만 확정 | 적용 지점 2곳: ① `ask` 반환 컨텍스트 선별 ② nl2sql 스키마 선별 제공. 세부는 필수 논문 [1] 정독 후 |
| nl2sql 재시도 횟수 | 미정 (제약 하나 확정) | SQL 생성 실패 시 재시도 정책 (edge-cases.md T1). **재시도는 실행 오류 메시지 같은 외부 신호를 반드시 물려야 하고, LLM 자체 검토 루프는 금지** — Huang et al., "LLMs Cannot Self-Correct Reasoning Yet" (ICLR 2024): 외부 피드백 없는 자기교정은 성능을 떨어뜨린다. **따라서 air의 `retryPlugin`(동일 입력 재호출)은 쓸 수 없고 도구 핸들러 안에서 직접 구현해야 한다** |
| 유사도 임계값 | 미정 | vector_search "관련 문서 없음" 판정 기준 (edge-cases.md T4). 데이터셋 역산 아닌 일반 기준으로 |
